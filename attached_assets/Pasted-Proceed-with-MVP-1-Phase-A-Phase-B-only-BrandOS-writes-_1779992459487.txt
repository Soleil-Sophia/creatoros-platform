Proceed with MVP-1 Phase A + Phase B only: BrandOS writes a Brand Profile to localStorage, and ContentOS Generate reads it.

Scope lock:
- Do NOT implement Phase C yet.
- Do NOT change ContentOS Library persistence.
- Do NOT connect Generate → Library yet.
- Do NOT add backend, auth, Supabase, AI API, Notion, publishing, or analytics.
- Do NOT redesign BrandOS or ContentOS.
- Do NOT modify marketing pages, Authority Engine, Dashboard, Modules, routes, or design tokens unless absolutely required.
- Keep changes minimal, reversible, and localStorage-first.

Goal:
Create the first real CreatorOS module handoff:

BrandOS Setup
→ saves Brand Profile
→ ContentOS Generate reads Brand Profile
→ Generate defaults reflect saved brand tone
→ UI shows a passive Brand Voice indicator

Implementation requirements:

1. Create a new Brand Profile library:
- src/app/lib/brand-profile/types.ts
- src/app/lib/brand-profile/storage.ts
- src/app/lib/brand-profile/defaultBrandProfile.ts

Use storage key:
creatoros-brand-profile-v1

BrandProfile shape:

type BrandProfile = {
  tone: string;
  complexity: string;
  formality: string;
  energy: string;
  voiceLabel?: string;
  updatedAt?: string;
};

Add safe localStorage read/write helpers:
- readBrandProfile()
- writeBrandProfile(profile)
- clearBrandProfile() if useful
- createVoiceLabel(profile)

Use try/catch and safe fallback behavior. Do not crash on malformed localStorage data.

2. Update BrandOS setup page:
File:
src/app/pages/brand-os-app.tsx

Convert the 4 existing uncontrolled inputs to controlled state:
- tone
- complexity
- formality
- energy

On mount:
- hydrate from readBrandProfile()
- otherwise use existing visible defaults if present

Make “Save Configuration” functional:
- saves BrandProfile to localStorage
- sets updatedAt
- creates voiceLabel
- shows a subtle “Saved” indicator or existing toast if the project already has one

Do not wire “Lock” buttons.
Do not implement draft mode.
Do not add complex validation.
Do not redesign the screen.

3. Update ContentOS Generate:
File:
src/app/screens/generate.tsx

On first mount:
- read BrandProfile from localStorage
- if profile exists, use profile.tone as the default tone instead of hardcoded “Conversational”
- do not override tone after user manually changes it during the same session

Add a passive Brand Voice indicator somewhere appropriate in the existing Generate UI:
- “Brand Voice: {voiceLabel}”
- If no profile exists: show subtle helper “No Brand Profile connected yet. Set up BrandOS first.” with a link to BrandOS if a matching route already exists.

Do not redesign Topbar or Generate layout.
Do not change output mock generation yet.
Do not add AI.

4. Optional small shared component:
Only if it keeps code cleaner, create:
src/app/components/shared/BrandVoiceChip.tsx

It should be passive/display-only.

5. Build and verify:
Run build/typecheck if available.

Manual verification:
- Open BrandOS setup
- Change tone/complexity/formality/energy
- Click Save Configuration
- Open ContentOS Generate
- Confirm tone defaults to saved BrandOS tone
- Confirm Brand Voice chip/helper appears
- Reload ContentOS Generate and confirm profile still loads
- Confirm malformed localStorage data does not crash the app
- Confirm existing routes still work
- Confirm Authority Engine Labs route still works

Report back:
- Files changed
- Build status
- Verification status
- Any risks or follow-up decisions

Important:
This is the first real CreatorOS handoff. Keep it small and clean.

Do not build more than:
BrandOS localStorage write
+ ContentOS Generate read
+ passive visible Brand Voice indicator